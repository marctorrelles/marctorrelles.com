name = "marctorrelles"
main = ".open-next/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".open-next/assets"
binding = "ASSETS"

# Custom domain is set in the Cloudflare dashboard (Workers > marctorrelles >
# Settings > Domains & Routes) once the Vercel DNS is removed. See CLAUDE.md.
